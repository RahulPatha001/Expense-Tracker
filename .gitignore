# General ignores (common to most projects)
*.log
tmp/
*.tmp

# Operating System generated files
.DS_Store       # macOS
Thumbs.db       # Windows
ehthumbs.db     # Windows
Desktop.ini     # Windows

# IDE specific files
.idea/          # IntelliJ IDEA project files
*.iml           # IntelliJ IDEA module files
*.ipr           # IntelliJ IDEA project files
*.iws           # IntelliJ IDEA workspace files
.vscode/        # VS Code specific files and settings
.apt_generated/ # Eclipse/STS annotation processing output
.classpath      # Eclipse
.factorypath    # Eclipse
.project        # Eclipse
.settings/      # Eclipse
.springBeans    # Spring STS specific file
.sts4-cache/    # STS cache

# NetBeans specific files
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# Build tool output directories
# These directories contain compiled code, temporary files, and artifacts
# generated by Maven, Gradle, etc., and should almost always be ignored.
target/
build/

# Java specific ignores
# These are often found within 'target/' or 'build/', but good to have as a fallback.
*.jar
*.war
*.ear
*.class

# Maven specific excludes (already covered by target/, but good for clarity)
# .mvn/wrapper/maven-wrapper.jar is usually KEPT, so the '!' is correct.
# However, the general 'target/' and 'build/' rules are usually sufficient
# to ignore the *contents* of these directories.
# The '!' for maven-wrapper.jar means "do NOT ignore this specific file".
!.mvn/wrapper/maven-wrapper.jar

# Python specific ignores
__pycache__/    # Python compiled bytecode
*.pyc
*.pyd           # Python DLLs
*.pyo           # Optimized Python bytecode
.Python/        # Old virtualenv or Python build related
env/            # Common virtual environment folder
venv/           # Common virtual environment folder
pip-log.txt     # Pip log file
pip-delete-this-directory/ # Pip temporary directory

# Add any other specific files or directories you know should be ignored.
# For example, local configuration files:
# config.local.py
# .env